@model IEnumerable<Tcc_Senai.Models.UnidadeCurricular>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutSenai.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Cadastrar Novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeUnidadeCurricular)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeUnidadeCurricular)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdUc">Editar</a> |
               
                <a asp-action="Delete" asp-route-id="@item.IdUc">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
